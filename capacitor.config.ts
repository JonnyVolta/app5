import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mate365.mx.app5',
  appName: 'ax²+bx+c=0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
